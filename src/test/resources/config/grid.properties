# Containerized grid (docker-android). See docker/README.md — requires a Linux host
# with KVM. Each device container exposes its own Appium port; point appium.url at
# the device you want (or override per run: APPIUM_URL=http://localhost:4724).
appium.url=http://127.0.0.1:4723
device.name=Android Emulator

# Example of provider-specific capabilities (uncomment as needed):
#capability.appium:udid=emulator-5554
