# Cloud device farm (BrowserStack App Automate shown as example).
# Credentials must NOT live in this file — pass them via environment variables:
#   APPIUM_URL=https://<USER>:<KEY>@hub-cloud.browserstack.com/wd/hub
#   APP_PATH=bs://<app-id-returned-by-the-upload-api>
# The keys below only document the shape of a farm run.
appium.url=https://hub-cloud.browserstack.com/wd/hub
device.name=Samsung Galaxy S23

# Provider-specific capabilities are forwarded as-is by DriverFactory:
capability.appium:platformVersion=13.0
#capability.bstack:projectName=Hermes
